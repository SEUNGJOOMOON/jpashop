
#db ??
spring:
  datasource:
    url: jdbc:h2:mem:test #인메모리방식 DB
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      #??? ???? ?? ?? ? ?? ??
      ddl-auto: create
    properties:
        #??? System out?? ?? ????? ???? ???.
        show_sql: true
        format_sql: true

  logging:
    level:
      #sql? logger? ??
      org.hibernate.SQL: debug